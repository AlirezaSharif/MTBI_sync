player_name: "Frazor"  # The exact name as it appears in the CSV 'Player' column
output_plot: "player_season_timeline.png"

# List of CSV files generated by the previous tool
files:
  - path: "output/test_stats.csv"
  - path: "output/kelston_stats.csv"

